{
	"name": "@crustjs/schema-utils",
	"version": "0.0.0",
	"description": "Internal Crust workspace package. Do not depend on directly — use @crustjs/validate.",
	"type": "module",
	"license": "MIT",
	"author": "chenxin-yan",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/chenxin-yan/crust.git",
		"directory": "packages/schema-utils"
	},
	"files": [
		"dist"
	],
	"exports": {
		".": {
			"import": "./dist/index.js",
			"types": "./dist/index.d.ts"
		}
	},
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"build": "bunup",
		"dev": "bunup --watch",
		"check:types": "tsc --noEmit",
		"test": "bun test",
		"publish": "bun publish --no-git-checks || true"
	},
	"devDependencies": {
		"@crustjs/config": "workspace:*",
		"@crustjs/core": "workspace:*",
		"bunup": "catalog:",
		"effect": "3.14.2",
		"zod": "^4.0.0"
	},
	"peerDependencies": {
		"typescript": "catalog:"
	},
	"dependencies": {
		"@crustjs/core": "workspace:*",
		"@standard-schema/spec": "^1.1.0"
	}
}
