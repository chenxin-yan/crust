---
"@crustjs/core": minor
---

Add `.derive(name, setup)` for Context values built from earlier Contexts on a command path. Derived values preserve Context inference, inheritance, duplicate-name validation, and native disposal.
