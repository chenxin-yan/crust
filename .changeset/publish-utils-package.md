---
"@crustjs/utils": minor
---

Publish `@crustjs/utils` as an internal runtime dependency with per-module subpath exports (`@crustjs/utils/<module>`). This is a fresh package that supersedes the unrelated 0.0.x releases.
