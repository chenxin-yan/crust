---
"@crustjs/core": minor
"@crustjs/testing": minor
---

Rename the typed invocation APIs `CommandShape` and `CommandShapeAt` to `CommandContract` and `CommandContractAtPath`, and rename the invalid-alias diagnostic key from `FIX_ALIAS_SHAPE` to `FIX_INVALID_ALIAS`.
