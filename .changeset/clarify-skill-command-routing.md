---
"@crustjs/skills": patch
---

Generate a single-file command reference by embedding all commands (including nested commands) directly in SKILL.md and removing command-index.md. Also clarify executable routing by documenting that any command labeled `runnable` (including `runnable, group`) can be executed.
