---
"@crustjs/extensions": minor
---

Make update notices command-less by default, require an explicit install scope to generate a package-manager command, and support linking to update documentation.
