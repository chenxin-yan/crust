---
"@crustjs/skills": minor
---

Add `writeSkills()` to render generated and authored skills into a self-describing, package-ready skill source without installing them. Hand-authored skill directories are included via `extras`; `version` is optional and only recorded in the generated SKILL.md `metadata` block.
