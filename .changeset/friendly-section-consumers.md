---
"@crustjs/core": minor
---

Allow documentation section audiences to accept Extension and renderer factory objects directly, so authors can write `only: [skill]` while branded `ExtensionId` values remain supported for custom renderers.
