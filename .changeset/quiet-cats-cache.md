---
"@crustjs/extensions": minor
---

Enable update-notifier persistence by default using a lazily loaded `@crustjs/store` cache at `stateDir(packageName)`. Set `cache: false` to opt out or continue providing a custom adapter.
