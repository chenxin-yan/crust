---
"@crustjs/man": minor
---

Add `man(options?)`, a build-only Extension that writes an mdoc page under the build output's `man` directory. Manual section 1 is the default; `man({ section })` selects another section. `writeManPage()` remains available for custom build pipelines.
