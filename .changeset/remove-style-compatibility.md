---
"@crustjs/style": minor
---

Remove the deprecated `rgb`, `bgRgb`, `hex`, and `bgHex` style methods and exports, along with the deprecated dynamic RGB and hex color helpers. Use the depth-aware `fg` and `bg` APIs instead.
