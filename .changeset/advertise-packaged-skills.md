---
"@crustjs/skills": minor
---

Advertise every packaged skill in root help and generated man pages with its description and resolved source path.

`resolveSkillSource()`, `readSkillFrontmatter()`, and `loadPackagedSkills()` are now synchronous so help sections can render them at snapshot time.
