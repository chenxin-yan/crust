---
"@crustjs/skills": patch
---

Improve `skillPlugin()` auto-install messaging to clearly distinguish first-time installs from updates. Auto-installs now print an explicit notification, and when the interactive command is enabled, the message includes a `my-cli skill` management hint.
