---
"@crustjs/skills": minor
---

Replace runtime generation, the mutable canonical store, and link install modes with copy-only installation from packaged skill sources. Remove the public `annotate()` API and `./annotations` export; author command instructions with `meta.sections` instead.
