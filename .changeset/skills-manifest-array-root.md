---
"@crustjs/skills": patch
---

An installed `crust.json` with an array root is now reported as malformed with reason `not-an-object` instead of `missing-version`, matching the rendered error message.
