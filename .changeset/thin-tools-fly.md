---
"create-crust": patch
---

Use the detected package manager for generated README and next-step commands independently of the selected runtime. Include an install command when dependency installation is skipped, and document installation for freshly cloned projects.
