---
"@crustjs/core": minor
"@crustjs/extensions": patch
"@crustjs/man": patch
"@crustjs/skills": patch
---

Default `ExtensionFactory` dependency, provider, flag, and command parameters to closed contribution sets. Published factories can now omit empty trailing type arguments; explicitly use the broad upper-bound type for namespaces that must remain open.
