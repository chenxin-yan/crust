---
title: Overview
description: What crust is, why crust exists, and where to start.
icon: House
---

import { Cards, Card } from "fumadocs-ui/components/card";

## What is Crust?

Crust is **a TypeScript-first, Bun-native framework for building command-line applications**. You declare commands with a chainable builder, and Crust provides end-to-end type safety through type inference. Everything beyond the core ships as composable modules, so you opt into first-class features and primitives only as you need them.

## Why Crust?

Most CLI tools in the TypeScript ecosystem are either **minimal arg parsers** that leave you wiring everything together, or **heavyweight frameworks** that bring more complexity than you need.

Crust sits in the sweet spot: more structured than a bare parser, lighter than a full framework, and designed so you only pull in what you actually use.

## Quick example

A `greet` command with a defaulted positional, a `--shout`/`-s` boolean flag, and help/version wired by two Extensions:

<include lang="ts" meta='title="src/cli.ts"'>
  ../../examples/landing/greet.ts
</include>

```sh
$ greet Ada --shout
HELLO, ADA!
$ greet --help
```

## Philosophy

- **Zero-dependency core.** The core has no runtime package dependencies.
- **Declarative command trees.** Define commands, arguments, flags, and subcommands together. Crust handles parsing and routing.
- **Composable features.** Add optional modules as you need them, including standalone libraries you can use without the framework.
- **End-to-end type safety.** Types flow from definitions to actions, Contexts, and programmatic invocation, with no manual annotations, decorators, or codegen.
- **Compile-time validation first.** Check statically known definitions through TypeScript, without repeating those checks at runtime. Reserve runtime validation for terminal input and definitions that cannot be checked statically.
- **One source of truth.** Reuse command definitions for parsing, inferred types, and generated help, completion scripts, manual pages, and agent documentation.
- **Explicit extension points.** Add application-wide behavior through Extensions with flags, commands, lifecycle hooks, and build hooks.

## Feature highlights

- **Commands and input parsing.** Nested [commands](/docs/guide/commands), positional [arguments](/docs/guide/arguments), and [flags](/docs/guide/flags) with aliases, defaults, choices, [Standard Schema validation](/docs/guide/types#schema-backed-definitions), and inferred types.
- **[Extensions](/docs/guide/extensions).** Shared flags, commands, and lifecycle hooks, plus [official extensions](/docs/modules/extensions) for help, version, typo suggestions, and update notices.
- **[Dependency injection](/docs/guide/contexts).** Typed Contexts with lazy initialization and automatic cleanup.
- **Generated from your definitions.** [Shell completion](/docs/modules/extensions/completion) for Bash, Zsh, and Fish, [manual pages](/docs/modules/man), and [agent skills](/docs/modules/skills).
- **Terminal UI.** [Styling](/docs/modules/style), [interactive prompts](/docs/modules/prompts), and [progress indicators](/docs/modules/progress), with non-interactive fallbacks.
- **[Testing helpers](/docs/guide/testing).** Capture output and exit codes, or drive interactive commands with simulated input.
- **[Build and distribution](/docs/guide/build).** Standalone Bun or Deno executables, or a Node.js bundle.

[`@crustjs/tui`](/docs/modules/tui) is Bun-only because its OpenTUI dependency uses native FFI.

## Roadmap

Curious about what's coming next? Check out the [Roadmap](https://github.com/users/chenxin-yan/projects/10) to see what we're working on, what's planned, and where you can contribute.

## Community

Have questions or want to chat about Crust? Join our [Discord community](https://discord.gg/sQF8hdN6Ht) to connect with other developers, get help, and stay up to date.

## What's next?

<Cards>
  <Card
    title="Quick Start"
    description="Build your first CLI in 2 minutes"
    href="/docs/quick-start"
  />
  <Card
    title="Guide"
    description="Learn how to define commands, arguments, flags, and more"
    href="/docs/guide"
  />
  <Card
    title="Modules"
    description="Explore the composable module ecosystem"
    href="/docs/modules"
  />
  <Card title="API Reference" description="Full API signatures and types" href="/docs/api" />
</Cards>
