{
	"extends": "../packages/config/tsconfig.base.json",
	"compilerOptions": {
		"declaration": false,
		"isolatedDeclarations": false
	},
	"include": ["."],
	// Standalone benchmark project with its own deps and `bun run check`.
	"exclude": ["cli-comparison"]
}
