name: CI Create Crust

on:
  pull_request:
    branches:
      - main
    paths:
      - "packages/config/**"
      - "packages/core/**"
      - "packages/create/**"
      - "packages/create-crust/**"
      - "packages/crust/**"
      - "packages/extensions/**"
      - "packages/man/**"
      - "packages/prompts/**"
      - "packages/style/**"
      - "packages/utils/**"
      - "package.json"
      - "bun.lock"
      - "turbo.json"
      - "tsconfig*.json"
      - ".github/workflows/ci-create-crust.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  smoke:
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun ci

      - name: Restore Turbo cache
        id: turbo-cache
        uses: actions/cache/restore@v6
        with:
          path: .turbo
          key: turbo-create-crust-${{ runner.os }}-${{ github.sha }}
          restore-keys: |
            turbo-create-crust-${{ runner.os }}-

      - name: Build create-crust dependency subset
        run: bun turbo run build --filter=./packages/style --filter=./packages/prompts --filter=./packages/core --filter=./packages/extensions --filter=./packages/config --filter=./packages/man --filter=./packages/create --filter=./packages/create-crust

      - name: Build Crust JavaScript CLI
        run: bun run --cwd packages/crust build:cli

      - name: Print tool versions
        run: |
          node --version
          npm --version
          bun --version

      - name: Run create-crust smoke test
        run: bun run --cwd packages/create-crust test:smoke
        env:
          CREATE_CRUST_SMOKE: "1"

      - name: Upload smoke workspace on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: create-crust-smoke-${{ matrix.os }}
          path: ${{ runner.temp }}/create-crust-smoke
          if-no-files-found: ignore

      # Cache save runs as a normal step (not a post-step) so a Windows
      # tar/zstd flake during compression is contained by continue-on-error
      # and cannot fail the job. The cache is purely a perf optimization.
      - name: Save Turbo cache
        if: always() && steps.turbo-cache.outputs.cache-hit != 'true'
        continue-on-error: true
        uses: actions/cache/save@v6
        with:
          path: .turbo
          key: turbo-create-crust-${{ runner.os }}-${{ github.sha }}
